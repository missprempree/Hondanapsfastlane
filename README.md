
  # NAPS : Invoice Key-In & Fast Lane Booking

  This is a code bundle for NAPS : Invoice Key-In & Fast Lane Booking. The original project is available at https://www.figma.com/design/vQaDsfROj2VuingD8paif9/NAPS---Invoice-Key-In---Fast-Lane-Booking.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  